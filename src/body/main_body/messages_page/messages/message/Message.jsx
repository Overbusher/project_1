
export const Message = (props) => {
    // const location = useLocation();
    // const ident ="/message/" + ' ' + props.id;
    // console.log(ident);
    return (
        <div>
            {props.message}
        </div>
    )
}