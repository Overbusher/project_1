export const FUser = (props) => {
    debugger
    return (
        <div>
            <div>{props.fname}</div>
            <div>{props.lname}</div>
            <div>{props.country}</div>
            <div>{props.city}</div>
        </div>
    )
}